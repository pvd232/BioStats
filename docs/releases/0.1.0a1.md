# VIPER 0.1.0a1 release candidate

## Candidate identity

| Value | Result |
|---|---|
| Package implementation commit | `582f723bfa3ab32c2785a9ad584f7fdb6a99a6aa` |
| Wheel | `viper_provenance-0.1.0a1-py3-none-any.whl` |
| Wheel SHA-256 | `05e315a61fc4068f0495f335e5af8ef36f5ef4de122f9877c8580a658fd2d736` |
| Source archive | `viper_provenance-0.1.0a1.tar.gz` |
| Source SHA-256 | `c62fa1549c321dfc10e371ef4538cca991f77030ccda8c88dd23f360b746a46a` |

`python -m twine check` accepted both files.

## Repository gate

The exact candidate passed the active-source repository gate in the `mantra`
Conda environment with Python 3.14.6:

```bash
ruff check viper tests examples/project/src tools
ruff format --check viper tests examples/project/src tools
pyright --pythonpath "$(command -v python)"
python tools/audit_contracts.py
python -m pytest tests -q
```

Ruff and Pyright reported zero errors. The contract audit passed with 10
contracts, 73 parsed Python blocks, 49 repeated classes, 8 repeated aliases,
136 generated Pydantic schemas, and 63 traced verifier rules. Pytest reported
219 passed tests, 6 hardware-gated skips, and 33 passed subtests in 403.70
seconds.

The parameter-namespace candidate awaits its Python 3.11–3.14 GitHub Actions
result.

## Installed-wheel gate

The exact wheel replaced the prior candidate in the `mantra` environment.
Python 3.14.6 imported VIPER from `site-packages`, exposed all 31 root public
names, returned capability JSON, and rendered the project-initialization
command.

| Python | Result |
|---|---|
| 3.11 | Repeat required |
| 3.12 | Repeat required |
| 3.13 | Repeat required |
| 3.14.6 | Pass |

The test package was then copied outside the source checkout. Python 3.14.6
imported VIPER from the installed environment's `site-packages` and completed
the generated acquisition plan, five-stage candidate, benchmark confirmation,
and terminal verification in 138.71 seconds.

## GCE gate

The parameter-namespace candidate requires a new live GCE run. The retained
machine image remains the approved provisioning source for that gate.

## Observed warnings

Two resume tests report TorchData's upstream `set_vital` deprecation warning.

## Publication blockers

Publication requires these remaining results and owner actions:

1. Pass Python 3.11–3.14 CI for the exact candidate commit.
2. Pass the live GCE gate with the exact wheel.
3. Select the package license and author metadata.
4. Provide TestPyPI credentials through the configured credential provider.
5. Authorize publication of the validated files to PyPI.
6. Provide the release-tag signing identity and configuration.

The files listed above are the current local candidate. A source or metadata
change requires a rebuild and repetition of the distribution, installed-wheel,
CI, and GCE gates.
