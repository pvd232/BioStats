# VIPER 0.1.0a1 release candidate

## Candidate identity

| Value | Result |
|---|---|
| Candidate source commit | `1d684b7f893fd820fa06150f365ccf46409300f6` |
| Wheel | `viper_provenance-0.1.0a1-py3-none-any.whl` |
| Wheel SHA-256 | `bdb3ce1209f3ba80a701800d79ccc49bcf993895177d5b7645230f0cf79eb669` |
| Source archive | `viper_provenance-0.1.0a1.tar.gz` |
| Source SHA-256 | `10047d7070bbff057cfde8214ec63efaca93c32722f0cd87f89d7836225dcb7e` |

`python -m twine check` accepted both files.

## Repository gate

The exact candidate passed the active-source repository gate in the `mantra`
Conda environment with Python 3.14.6:

```bash
make check-fast
make check-integration
python -m pytest tests -q -m release
```

The fast gate reported 165 passing tests, 61 deselections, and 11 passing
subtests in 41.23 seconds including lint and type checking. The integration
gate reported 54 passing tests, 171 deselections, and 22 passing subtests in
251.49 seconds. The release tier reported one passing generated-project test
in 144.20 seconds. Together, the three gates cover all 220 host-independent
tests.

GitHub Actions run
[`32794136388`](https://github.com/pvd232/BioStats/actions/runs/32794136388)
passed the redesigned validation system. The fast job completed in 163 seconds.
Its success started the integration, release-candidate, and Python 3.11–3.13
compatibility jobs. Every job passed.

## Installed-wheel gate

The exact wheel replaced the prior candidate in the `mantra` environment.
Python 3.14.6 imported VIPER from `site-packages` and exposed all 31 root public
names.

| Python | Result |
|---|---|
| 3.11 | Pass in 143-second compatibility job |
| 3.12 | Pass in 162-second compatibility job |
| 3.13 | Pass in 147-second compatibility job |
| 3.14 | Pass across fast, integration, and release-candidate jobs |

The local installed-wheel and external generated-project checks used Python
3.14.6.

The test package was then copied outside the source checkout. Python 3.14.6
imported VIPER from the installed environment's `site-packages` and completed
the generated acquisition plan, five-stage candidate, benchmark confirmation,
and terminal verification in 139.04 seconds.

The final manifest change removed the internal publication checklist from the
source archive. The rebuilt wheel preserved the byte identity of all 38 members
apart from its generated `RECORD` file. The final wheel then installed and
imported successfully from `site-packages`.

## GCE gate

The parameter-namespace candidate requires a new live GCE run. The retained
machine image remains the approved provisioning source for that gate.

## Observed warnings

Two resume tests report TorchData's upstream `set_vital` deprecation warning.
The copied external test package emitted two marker-registration warnings
because that isolated copy omitted the repository's `pyproject.toml`.

## Publication blockers

Publication requires these remaining results and owner actions:

1. Pass the live GCE gate with the exact wheel.
2. Select the package license. Author and maintainer metadata identify Peter
   Driscoll through the confirmed publication email.
3. Register `.github/workflows/release.yml` as the trusted publisher for the
   `testpypi` and `pypi` GitHub environments.
4. Provide the release-tag signing identity and configuration.
5. Approve the protected `pypi` environment after TestPyPI verification.

The files listed above are the current local candidate. A source or metadata
change requires a rebuild and repetition of the distribution, installed-wheel,
CI, and GCE gates.
