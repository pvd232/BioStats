name: mantra
channels:
  - defaults
dependencies:
  - pip
  - pydantic
  - pyyaml
  - validators
  - pytest
  - ruff
  - huggingface_hub
  - numpy
  - pytorch
  - pip:
      - pyright>=1.1.400,<2
      - torchdata==0.11.0
prefix: /Users/machina/miniconda3/envs/mantra
