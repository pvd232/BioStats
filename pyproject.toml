[build-system]
requires = ["setuptools>=75"]
build-backend = "setuptools.build_meta"

[project]
name = "viper-provenance"
version = "0.1.0"
description = "Reproducible execution and artifact provenance for computational experiments"
readme = "README.md"
requires-python = ">=3.11"
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Science/Research",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Programming Language :: Python :: 3.14",
    "Topic :: Scientific/Engineering :: Information Analysis",
    "Topic :: Software Development :: Libraries :: Python Modules",
]
dependencies = [
    "huggingface_hub>=1,<2",
    "numpy>=2,<3",
    "pydantic>=2.12,<3",
    "PyYAML>=6,<7",
    "torch>=2.6,<3",
    "torchdata==0.11.0",
]

[project.optional-dependencies]
test = [
    "pyright>=1.1.400,<2",
    "pytest>=9,<10",
    "ruff>=0.12,<1",
]
release = [
    "build>=1.2,<2",
    "twine>=6,<7",
]

[project.urls]
Repository = "https://github.com/pvd232/BioStats"

[project.scripts]
viper = "viper.cli:main"

[tool.ruff]
exclude = ["archive"]
target-version = "py311"

[tool.ruff.lint]
select = ["D", "E", "F", "I", "UP"]

[tool.ruff.lint.pydocstyle]
convention = "google"

[tool.pytest.ini_options]
pythonpath = [".", "examples/project/src"]

[tool.setuptools]
include-package-data = false

[tool.setuptools.packages.find]
where = ["."]
include = ["viper*"]
